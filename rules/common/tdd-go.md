# TDD for Go

1. Write a failing table-driven test for behavior.
2. Implement minimal code to pass.
3. Refactor for clarity and error context.
4. Re-run `go test ./...` and `go test -race ./...`.
5. Keep public API stable unless spec explicitly allows change.
