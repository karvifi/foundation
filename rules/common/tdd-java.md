# TDD for Java

1. Write a failing JUnit test for expected behavior.
2. Implement minimal production code to pass.
3. Refactor while preserving readability and boundaries.
4. Run `mvn test` or `gradle test` and static analysis.
5. Add integration test when behavior crosses IO/database boundaries.
